# add_aegis_dialect(dialect dialect_namespace)
add_aegis_dialect(Secret secret)

# add_aegis_doc(doc_filename output_file output_directory command)
#add_aegis_doc(Secret SecretDialect Secret/ -gen-dialect-doc)
#add_aegis_doc(Secret SecretOps Secret/ -gen-op-doc -dialect=Secret)
#add_aegis_doc(Secret SecretTypes Secret/ -gen-typedef-doc -dialect=Secret)